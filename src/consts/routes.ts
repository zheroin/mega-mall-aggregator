const ROUTES = {
  MAIN: '/',
  CATEGORY: '/categories/:type',
  LIST: '/categories/:type/:subtype/:item?',
  SEARCH: '/products',
  PRODUCT: '/products/:id',
  STORES: '/stores'
};

export { ROUTES };

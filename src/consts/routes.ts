const ROUTES = {
  MAIN: '/',
  CATEGORY: '/categories/:type',
  LIST: '/categories/:type/:subtype/:item?',
  SEARCH: '/products/search',
  PRODUCT: '/products/:id',
  STORES: '/stores'
};

export { ROUTES };
